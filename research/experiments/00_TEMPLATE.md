# EXP-NN — <short title of the experiment>

> status: pre-registered | running | supported | refuted | inconclusive · opened: YYYY-MM-DD · ticket: #NN · branch/PR: <ref>

> **This file is the template.** A real experiment is a directory: `experiments/EXP-NN_<slug>/EXPERIMENT.md` (this file, filled) **plus** `results/` (raw data as CSV/JSON, figures as PNG/SVG, and the plot script that turns the data into the figures). Copy, fill **Hypothesis through Reproducibility BEFORE running anything** (that's the pre-registration — it's what makes the results honest), add a `experiments[]` entry to [`../MANIFEST.json`](../MANIFEST.json), delete this blockquote. Experiment *code* lives in the repo proper (the experiment-harness path in `PROJECT_CONVENTIONS.md`) and ships through the normal branch → PR → CI flow; this directory holds the write-up + data + figures.

## Hypothesis

<One falsifiable sentence. Where it came from: `notes/<note>.md` claim `[tier]`, a paper (`source: <URL>`), or a decision need (`D-NN`).>

## Metrics & success criteria

<Defined BEFORE running — the audit checks this section exists; your discipline makes it honest:>
- **Metric(s):** <exactly what is measured, units, and how it's computed>
- **Success bar:** <the pre-committed threshold/margin that counts as "supported" — e.g. "≥X% improvement over baseline with non-overlapping variance across N runs">
- **Runs & variance plan:** <N repeats, seeds; close margins (within noise) are reported as ties, not wins>

## Method

- **Baseline:** <the named, fairly-tuned baseline — "no baseline" must be argued, not defaulted>
- **Variant(s):** <what changes, one variable at a time where possible>
- **Held fixed:** <data, seeds, hardware, hyperparameters — the controls>
- **Implementation:** <where the code lives (per `PROJECT_CONVENTIONS.md`), and any deliberate deviations from the source paper, stated>

## Reproducibility

<Everything needed to regenerate every number and figure from a cold checkout:>
- **Commands:** <the exact invocations, in order>
- **Seed(s):** <…> · **Commit:** <sha> · **Environment:** <hardware, key library versions>
- **Data:** <where `results/` raw data comes from / how it's regenerated>

## Results

<Filled AFTER running — never edit the sections above to fit what happened; deviations from the plan are recorded here, visibly:>
- **Comparison table** vs the baseline (mean ± variance over the N runs), and vs the source paper's reported numbers if implementing one — discrepancies stated, not smoothed over.
- **Figures** in `results/`, each regenerated by the committed plot script from the committed data.
- If the paper's numbers and ours disagree: the honest paragraph about why (implementation gap, data gap, or the paper overstating).

## Failure modes & limitations

<Where it breaks, where it wasn't tested, what doesn't generalize. An experiment with no known limitations wasn't probed hard enough.>

## Outcome

<supported / refuted / inconclusive — judged against the PRE-REGISTERED bar, plus what changes downstream: the `RR-NN` it feeds, the `D-NN` it grounds, the follow-up tickets filed (#NN). Refuted and inconclusive outcomes are recorded with the same care as wins.>
