# GLOSSARY — Term Definitions

The shared vocabulary of `<DOMAIN>`. Each term gets a one- or two-sentence definition pinned to the book(s) that cover it in depth, so a reader can look up a word and then go read about it. This is the canonical place a term is defined once; books refer back here rather than redefining.

> **Format.** Alphabetize under single-letter `## A`, `## B`, … headers. One entry per term: `**Term** — definition. (Book NN §X)`. Keep definitions tight and domain-true; the parenthetical points at the fullest treatment. Write "Book NN" as plain text. To send a reader to a related reference doc, use a markdown link, e.g. [PATTERNS.md](PATTERNS.md).

## A

<!-- EXAMPLE — replace -->
**`<TERM>`** — A `<one-sentence definition in the domain's own words>`; the unit `<EXAMPLE_SYSTEM>` uses to `<do the thing>`. (Book NN §X.Y)

**`<ACRONYM>` (`<expansion>`)** — `<what it is and why it matters>`. Contrast with `<RELATED_TERM>`. (Book MM §X)
<!-- /EXAMPLE -->

## B

<!-- EXAMPLE — replace -->
**`<TERM_B>`** — `<definition>`. Often confused with `<TERM_A>`; the difference is `<the distinction>`. (Book MM §X.Y)
<!-- /EXAMPLE -->

## C

<!-- EXAMPLE — replace -->
**`<CORE_CONCEPT>`** — `<definition>`. The central abstraction of Volume `<N>`. (Book NN §X)
<!-- /EXAMPLE -->

<!-- Continue with ## D … ## Z. Omit letters with no terms. -->

---

> **Fill this in during onboarding.** Seed this from the `key_concepts` of each book as it is written, then keep it as the single source of truth for definitions. Rules: define each term exactly once here; cite the section that covers it most fully (verify the `§` against `SECTIONS.json`); prefer the domain's real terminology over invented words; when two terms are easily confused, say so in both entries. If a term is used in a book but not defined here, that is a gap to close.
