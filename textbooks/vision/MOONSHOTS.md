# MOONSHOTS — Ambitious Long-Shots

The big, hard, maybe-impossible things `<DOMAIN>` could become if the hard problems were solved — the ideas worth chasing even if most fail. This is deliberately speculative; the grounded near-term view is in [FUTURES.md](FUTURES.md).

> **Format.** A short list of `## Moonshot N` entries, numbered. Each: a bold one-line ambition, a few sentences on what it would take and why it would matter, and an honest note on what currently blocks it. Dream big but stay honest about the obstacle. Keep the whole doc to about a page. Link a sibling doc, e.g. [FUTURES.md](FUTURES.md).

## Moonshot 1 — `<The Bold Ambition>`

<!-- EXAMPLE — replace -->
`<The audacious end state — e.g. <EXAMPLE_SYSTEM> that fully <does the currently-impossible thing> with no human in the loop>`. If it worked, `<the payoff — who it changes and how>`. What stands in the way today: `<the honest blocker — the unsolved problem, the missing primitive, the cost wall>`.
<!-- /EXAMPLE -->

## Moonshot 2 — `<A Second Long-Shot>`

<!-- EXAMPLE — replace -->
`<The ambition>`. It would require `<the breakthrough needed>`, which is `<why that is hard>`. Even partial progress would `<the consolation prize that makes it worth attempting>`.
<!-- /EXAMPLE -->

<!-- Add 2–5 moonshots. It is fine for these to be improbable — that is the point. -->

---

> **Fill this in during onboarding.** These are the inspirational far edge — write them to stretch the reader's sense of what is possible in `<DOMAIN>`. The discipline that keeps them from being empty hype: for each one, name the *specific* thing that currently makes it impossible. A moonshot with a named blocker is a research agenda; one without is a slogan.
