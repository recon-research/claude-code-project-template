# PROLOGUE — Why This Matters

A short "why should anyone care about `<DOMAIN>`?" framing — half a page that sets the stakes before the curriculum begins. It is the morale and motivation document: what becomes possible when you understand this, and why it is worth the effort.

> **Format.** A few short paragraphs of prose (no headings needed). Open with the tension or opportunity in `<DOMAIN>`, name what mastery unlocks, and end on why *now* is the moment. Keep it honest — inspiration, not hype. Roughly half a page. To point readers onward, link a real doc, e.g. [../reference/STARTER_KIT.md](../reference/STARTER_KIT.md).

<!-- EXAMPLE — replace -->
`<DOMAIN>` sits at a point where `<the core tension — e.g. demand for X is exploding while the tools to build it remain hard to wield>`. Most people who touch it `<the common shallow outcome — e.g. wire together libraries they don't understand and hit a wall>`. The few who understand `<the underlying ideas>` can `<the leap — e.g. build systems an order of magnitude more capable, and reason about why they work>`.

This library exists to move you from the first group to the second. It builds one running example, `<EXAMPLE_SYSTEM>`, from `<the starting point>` to `<the end state>` — so the ideas arrive attached to something real, not as abstractions. By the end you should be able to `<the concrete capability the reader gains>`, and to tell when the conventional approach is wrong.

Why now: `<the timing argument — e.g. the tooling just got good enough, the demand just got real, an agent can now carry the boilerplate so you can spend your attention on judgment>`. The path starts at [../reference/STARTER_KIT.md](../reference/STARTER_KIT.md).
<!-- /EXAMPLE -->

---

> **Fill this in during onboarding.** Write this last, once you know the domain's real stakes and the example system's name. The test of a good prologue: a smart newcomer reads half a page and wants to start. Be specific to `<DOMAIN>` and honest about effort — overselling here costs trust everywhere else.
