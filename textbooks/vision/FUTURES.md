# FUTURES — Near-Term Directions

Where `<DOMAIN>` is plausibly headed in the next year or two — the developments a practitioner should watch and could realistically build toward. Grounded extrapolation, not fantasy; the ambitious long-shots live in [MOONSHOTS.md](MOONSHOTS.md).

> **Format.** A short list of `###` entries, each a near-term direction: a one-line name, two or three sentences on what is changing and why it matters, and (optionally) the in-library section it builds on as plain-text "Book NN §X". Keep it tight — a page at most. Link sibling vision docs, e.g. [MOONSHOTS.md](MOONSHOTS.md).

<!-- EXAMPLE — replace -->
### `<Near-term direction A>`

`<What is shifting — e.g. <technique X> is moving from research into mainstream tooling>`. For `<DOMAIN>` this means `<the practical consequence>`. Builds on the foundations in Book NN.

### `<Near-term direction B>`

`<The shift>`, which makes `<the previously-hard thing>` newly feasible. Watch `<the signal that tells you it has arrived>`.
<!-- /EXAMPLE -->

<!-- Add 3–6 entries. Each should be something a reader could act on within a year or two. -->

---

> **Fill this in during onboarding.** Populate this from the "Bleeding edge" and "Gaps & opportunities" sections of the books — the near-term futures are usually the gaps that are about to close. Keep entries credible and dated-feeling; revisit them as the field moves. If something is a decade out or speculative, it belongs in [MOONSHOTS.md](MOONSHOTS.md), not here.
